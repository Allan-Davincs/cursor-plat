# Frontend (InfoX Commerce)

This frontend is part of the full-stack InfoX Commerce platform.

Use the root project README for full architecture and setup details:

- `../README.md`

Quick start:

```bash
cp .env.example .env
npm install
npm run dev
```
